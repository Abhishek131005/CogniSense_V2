# Install this on top of backend/requirements.txt when you want to run
# the full standalone webcam-based oculomotor pipeline locally.
#
# Example:
#   pip install -r backend/requirements.txt -r backend/requirements-oculomotor.txt

absl-py==2.1.0
contourpy==1.3.2
cycler==0.12.1
flatbuffers==23.5.26
fonttools==4.53.1
kiwisolver==1.4.5
matplotlib==3.8.4
mediapipe>=0.10.13,<0.11
# GUI-enabled OpenCV packages are required for fullscreen camera tasks.
pandas==2.2.2
pyparsing==3.1.2
python-dateutil==2.9.0
sounddevice==0.4.6
tzdata==2024.1
